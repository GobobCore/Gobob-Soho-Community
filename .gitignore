# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Node
node_modules/
.next/
out/
dist/

# Env / secrets
.env
*.local

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Data / runtime
data/
*.db
logs/

# Backup
*_backup_v018*
*.bak
*.bak-*

# build 缓存
*.tsbuildinfo

# ═══════════════════════════════════════════════════════════
# SaaS 闭源 (cloud/) — 不上 GitHub
# ═══════════════════════════════════════════════════════════
# cloud/ 是 SaaS 运营版 (多机构 + 计费 + 运营后台), 商业资产, 闭源
# 只推 intsch (内部 Gitea), 不推 GitHub
# 注: 现有生产目录 backend/ portal/ app/ soho-ops/ 是 SaaS 版当前形态,
#     也是闭源, 但它们已经在 GitHub 历史里 (之前 push 过), 暂不撤回
#     (要彻底清理需要 force-push 重写历史, 风险大, 后续再做)
cloud/
